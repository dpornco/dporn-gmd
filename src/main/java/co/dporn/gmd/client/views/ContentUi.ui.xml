<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
	xmlns:m='urn:import:gwt.material.design.client.ui'
	xmlns:ma="urn:import:gwt.material.design.addins.client">
	<ui:style>
	.container {
		padding-left: 250px;
		padding-right: 140px;
		padding-top: 100px;
	}
	
	.label {
		font-size: 2em;
		font-weight: bold;
		margin-left: 12px;
	}
	
	.row {
		min-height: 335px;
	}
	</ui:style>
	<!-- Container -->
	<m:MaterialContainer ui:field="mainContent"
		addStyleNames="{style.container}">

		<!-- Cards -->
		<m:MaterialLabel text="Featured Channels"
			addStyleNames="{style.label}" />
		<m:MaterialRow ui:field="featuredChannels"
			addStyleNames="{style.row}" />

		<!-- Cards -->
		<m:MaterialLabel text="Featured Posts"
			addStyleNames="{style.label}" />
		<m:MaterialRow ui:field="featuredPosts"
			addStyleNames="{style.row}" />

		<!-- Cards -->
		<m:MaterialLabel text="Recent Posts"
			addStyleNames="{style.label}" />
		<m:MaterialRow ui:field="recentPosts"
			addStyleNames="{style.row}" />

	<m:MaterialPanel ui:field="bottom"></m:MaterialPanel>

	</m:MaterialContainer>
</ui:UiBinder> 